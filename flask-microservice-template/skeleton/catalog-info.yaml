piVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: ${{ values.name }}
  description: ${{ values.description }}
  annotations:
    github.com/project-slug: ${{ values.destinationowner }}/${{ values.destinationrepo }}
spec:
  type: service
  owner: ${{ values.owner }}
  lifecycle: experimental
